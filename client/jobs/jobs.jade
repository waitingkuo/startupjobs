template(name="jobList")
  #job-list
    .row-fluid
      h2 職缺
      .jobs
        each jobs
          +jobListItem
            
template(name="jobDetail")
  with job
    .row-fluid#job-detail
      .container-fluid
        .card
          .title {{jobTitle}}
          p {{salaryLowerBound}} ~ {{salaryUpperBound}}
        .card
          .title 公司簡介
          p {{company}}
          p {{location}}
          p 
        .card
          .title 職缺說明
          p {{description}}

template(name="submitJob")
  .row-fluid
    .container-fluid
      +quickForm collection="Jobs" type="insert" id="submit-job"

template(name="updateJob")
  .row-fluid#update-job
    .container-fluid
      +quickForm collection="Jobs" doc=job type="update" id="update-job"

template(name="jobListItem")
  a(href="{{pathFor 'jobDetail'}}")
    .job
      .row
        .col-xs-9
          p 
            span {{jobTitle}} 
            span {{company}}
          p 
            span 0 point
        .col-xs-3
          span {{salaryLowerBound}} ~ {{salaryUpperBound}}
          a.pull-right.btn.btn-info(href="{{pathFor 'updateJob'}}") 編輯
